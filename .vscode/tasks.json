{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/Alpha_Sharp.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}